function loadNotes(){const notes=JSON.parse(localStorage.getItem('notes')||'[]');const list=document.getElementById('notes-list');list.innerHTML='';notes.forEach((note,i)=>{const el=document.createElement('div');el.className='note';el.innerHTML=`<div><strong>${note.title||'Note'}</strong><p>${note.content||''}</p></div><div><button onclick="deleteNote(${i})">Delete</button></div>`;list.appendChild(el)});}function addNote(){const input=document.getElementById('note-input');const val=input.value.trim();if(!val) return;let notes=JSON.parse(localStorage.getItem('notes')||'[]');notes.push({title:val.split('\n')[0],content:val});localStorage.setItem('notes',JSON.stringify(notes));input.value='';loadNotes()}function deleteNote(i){let notes=JSON.parse(localStorage.getItem('notes')||'[]');notes.splice(i,1);localStorage.setItem('notes',JSON.stringify(notes));loadNotes()}function searchNotes(){const q=document.getElementById('search').value.toLowerCase();document.querySelectorAll('.note').forEach(n=>{n.style.display=n.textContent.toLowerCase().includes(q)?'flex':'none'})}window.onload=loadNotes;
